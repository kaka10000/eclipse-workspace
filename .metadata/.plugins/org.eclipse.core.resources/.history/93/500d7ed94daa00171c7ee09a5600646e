一、测试FreeMaker语法
1.1、FreeMaker的介绍
	FreeMaker是一种模板引擎，它可以根据后端提供的数据，渲染模板的变量。与velocity是一样的，但是velocity是更加轻量级的模板渲染引擎，但是velocity支持的语法不如FreeMaker强大，在velocity在早期的阿里、58用的比较多，而且经过阿里开发人员的测试，发现速度非常快。
1.2、FreeMaker环境搭建
	在springboot的项目中，要引入的FreeMaker的坐标
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-freemarker</artifactId>
		</dependency>
	注意：在平常的web项目中(非web框架中)，一般引入如下包即可。
	<dependency>
	    <groupId>org.freemarker</groupId>
	    <artifactId>freemarker</artifactId>
	    <version>2.3.9</version>
	</dependency>
	

springboot+mybatis+freemaker整合如何搭建：http://www.jianshu.com/p/ee0718a32919